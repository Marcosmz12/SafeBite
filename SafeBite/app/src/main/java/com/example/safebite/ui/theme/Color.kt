package com.example.safebite.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Paleta Principal (Light) ───────────────────────────────────────────────
val GreenPrimary     = Color(0xFF2E7D32)   // Primario fuerte
val GreenLight       = Color(0xFF4CAF50)   // Primario variante
val GreenSoft        = Color(0xFFE8F5E9)   // Fondo suave / surface tint
val AmberAccent      = Color(0xFFFFB300)   // Acento / Secondary

// ─── Paleta Dark ────────────────────────────────────────────────────────────
val SurfaceDark      = Color(0xFF1C1C1E)   // Fondo principal dark
val CardBackground   = Color(0xFF2A2A2E)   // Cards en dark

// ─── On-colors Light ────────────────────────────────────────────────────────
val OnPrimaryLight   = Color(0xFFFFFFFF)   // Texto sobre primario
val OnSecondaryLight = Color(0xFF1C1B1F)   // Texto sobre secundario
val BackgroundLight  = Color(0xFFF9FBF9)   // Fondo app light
val SurfaceLight     = Color(0xFFFFFFFF)   // Superficie tarjetas light
val OnSurfaceLight   = Color(0xFF1C1C1E)   // Texto sobre superficie light
val ErrorLight       = Color(0xFFB00020)   // Error light
val OnErrorLight     = Color(0xFFFFFFFF)

// ─── On-colors Dark ─────────────────────────────────────────────────────────
val OnPrimaryDark    = Color(0xFFFFFFFF)
val OnSecondaryDark  = Color(0xFFFFFFFF)
val BackgroundDark   = Color(0xFF121212)
val SurfaceDarkCard  = Color(0xFF2A2A2E)
val OnSurfaceDark    = Color(0xFFE6E1E5)
val ErrorDark        = Color(0xFFCF6679)
val OnErrorDark      = Color(0xFF370008)

// ─── Legado (mantén compatibilidad si las usas en otro lado) ────────────────
val Purple80         = Color(0xFFD0BCFF)
val PurpleGrey80     = Color(0xFFCCC2DC)
val Pink80           = Color(0xFFEFB8C8)
val Purple40         = Color(0xFF6650A4)
val PurpleGrey40     = Color(0xFF625B71)
val Pink40           = Color(0xFF7D5260)
